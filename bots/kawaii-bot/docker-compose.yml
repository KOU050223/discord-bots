version: '3.8'

services:
  kawaii-bot:
    build:
      context: ../../  # リポジトリルートをコンテキストに
      dockerfile: bots/kawaii-bot/Containerfile
      target: production
    container_name: kawaii-bot
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    env_file:
      - .env
    # ログ管理（オプション）
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
